services:
  - type: web
    name: growing-success-garden
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: INVITE_CODE
        sync: false
      - key: NODE_ENV
        value: production
